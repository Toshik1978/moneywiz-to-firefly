# Migrate MoneyWiz transactions to FireFly III
An application helps to migrate MoneyWiz transactions, using generated CSV file.
It can automatically parse account creation and transfer records. It doesn't properly support investments portfolio!

## Usage
1. Clone repository
2. Run application with `moneywiz-to-firefly`
